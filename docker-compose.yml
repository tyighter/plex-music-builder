version: "3.9"
services:
  plex-music-builder:
    image: plex-music-builder
    container_name: plex-music-builder
    volumes:
      - /mnt/appdatacache/appdata/pmb:/app
    working_dir: /app
    command: ["python", "start_services.py"]
    ports:
      - "4444:4444"
    restart: "no"
